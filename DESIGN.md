# LoopLoopTech 官网设计指导规范（DESIGN.md）

> 适用范围：www.looplooptech.com 公司官网及其两款 SaaS 产品（WMS / LIMS）的产品页。
> 本文档是所有视觉、交互、动效、文案与技术实现的唯一设计依据。

---

## 1. 品牌定位与设计基调

| 维度 | 定义 |
| --- | --- |
| 公司 | LoopLoopTech（循环科技）——面向企业的仓储、实验室与智慧社区数字化 SaaS 服务商 |
| 产品 | **WMS**（仓储管理系统）、**LIMS**（环境检测实验室信息管理系统）、**ICMS**（智慧社区管理系统）——三款独立产品 |
| 气质关键词 | 可信赖、工程感、精密、克制、现代 |
| 一句话基调 | 「像 DeepSeek 一样有工程美感，像 PixPin 一样干净利落，像 itsalin 一样克制专注」 |

### 三大参考站取材结论

- **pixpin.com** → 取其*版式与配色*：明亮主题、蓝主色、大留白、卡片化功能陈列、清晰的「特性 → 人群 → 证言 → CTA」落地页节奏。
- **itsalin.com** → 取其*克制*：内容居中、单一焦点、极简导航、独立开发者式的真诚语气（用于「关于我们」与文案调性）。
- **deepseek.com/harness** → 取其*动效与工程氛围*：滚动渐入、编号章节（01/02/03）、等宽字代码块、终端元素、深色科技质感、大标题 + 渐变点睛。

---

## 2. 设计原则（按优先级）

1. **内容优先，装饰克制** —— 动效服务于信息层级，不炫技。
2. **明暗双态同等一等公民** —— 每个组件必须同时验证 Light / Dark 两种主题。
3. **双语无阉割** —— 中英文任意切换，布局不得因语言切换而破版（文案长度差按 ±40% 预留）。
4. **8pt 网格** —— 所有间距、尺寸取 4/8 的倍数。
5. **一次一个焦点** —— 每屏只讲一件事（参考 itsalin）。
6. **性能即设计** —— 首屏 LCP < 2.5s，动效不阻塞交互，尊重 `prefers-reduced-motion`。

---

## 3. 色彩系统

### 3.1 品牌色

品牌色取自 Logo 描边色，禁止另行发明主色。

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--brand` | `#1E80FF` | 主色：链接、主按钮、高亮、Logo |
| `--brand-hover` | `#3D8EFF` | 主色 hover |
| `--brand-strong` | `#0B5FD9` | 主色按下/深色强调 |
| `--brand-soft` | `rgba(30,128,255,.10)` | 主色底色（徽章、卡片光晕） |

产品辅色（用于区分 WMS 与 LIMS，二者仅作为点缀，不与大结构冲突）：

| Token | 色值 | 归属 |
| --- | --- | --- |
| `--accent-wms` | `#00B383`（青碧） | WMS 仓储/物流意象 |
| `--accent-lims` | `#8B5CF6`（堇紫） | LIMS 实验室/科学意象 |
| `--accent-icms` | `#F59E0B`（琥珀） | ICMS 社区/服务意象 |

### 3.2 语义色

| Token | Light | Dark | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#FFFFFF` | `#0A0E1A` | 页面底色（暗色取深空蓝黑，非纯黑） |
| `--bg-subtle` | `#F6F8FB` | `#101624` | 区块底、卡片底 |
| `--bg-inset` | `#EEF2F7` | `#151C2E` | 代码块、内嵌面板 |
| `--fg` | `#0E1526` | `#E8EDF7` | 正文文字 |
| `--fg-muted` | `#5A6577` | `#93A0B8` | 次级文字 |
| `--fg-faint` | `#98A2B3` | `#5D6A85` | 辅助说明、页脚 |
| `--border` | `#E4E9F1` | `#1E2740` | 描边、分割线 |
| `--success` | `#12A150` | `#2ED573` | 成功态 |
| `--warning` | `#E8930C` | `#FFB930` | 警示态 |
| `--danger` | `#E5484D` | `#FF6369` | 危险态 |

### 3.3 渐变与光晕

- **Hero 渐变**：`linear-gradient(135deg, #1E80FF 0%, #8B5CF6 100%)` —— 仅用于 Logo 光晕、大标题点睛词、主按钮 hover 光。
- **网格背景**（DeepSeek 式工程感）：`--bg` 上叠加 1px 网格线（`--border` 的 40% 透明度），仅用于 Hero 与 CTA 区。
- **光斑（Glow）**：品牌色 12% 透明度、`blur(120px)` 的大圆，置于 Hero 右上/左下，暗色模式下增强至 18%。

---

## 4. 字体与排版

### 4.1 字体栈

| 层级 | 字体栈 | 说明 |
| --- | --- | --- |
| 正文 | `Inter, "PingFang SC", "HarmonicOS SC", "Microsoft YaHei", system-ui, sans-serif` | 中西混排自然 |
| 展示标题 | 同上，`font-weight: 700`，`letter-spacing: -0.02em` | 大标题微收紧 |
| 等宽 | `"JetBrains Mono", "SF Mono", Consolas, monospace` | 代码块、终端演示、数据看板、章节编号 `01/02` |

> 通过 `next/font` 加载 Inter 与 JetBrains Mono（本地子集化，避免布局抖动）；中文回退系统字体。

### 4.2 字号阶梯（桌面 / 移动）

| Token | 桌面 | 移动 | 用途 |
| --- | --- | --- | --- |
| `display` | 64px / 1.1 | 40px / 1.15 | Hero 主标题 |
| `h1` | 48px / 1.15 | 32px | 产品页主标题 |
| `h2` | 36px / 1.2 | 28px | 章节标题 |
| `h3` | 24px / 1.3 | 20px | 卡片标题 |
| `body-lg` | 18px / 1.7 | 16px | Hero 副文案 |
| `body` | 16px / 1.7 | 15px | 正文 |
| `body-sm` | 14px / 1.6 | 13px | 次级说明、徽章 |
| `mono-tag` | 13px / 1 | 12px | 章节编号、代码标签 |

### 4.3 双语排版规则

- 中文正文行高 ≥ 1.7；英文正文 1.6。
- 英文标题使用 `text-wrap: balance`；长词组件（按钮、徽章）英文允许 `nowrap` 但中文必须可换行。
- 标题点睛词使用品牌渐变文字（`background-clip: text`），中英文各自标记（如中文「循环」，英文 "loop"）。

---

## 5. 布局与间距

- **容器**：桌面 `max-width: 1152px`（72rem），移动全宽、左右 `16px` 内边距。
- **栅格**：特性区 3 列（`grid-cols-3`），1100px 以下 1 列；图文分栏区 5:7（文:图）。
- **区块节奏**：桌面区块间距 `96px`，区块内标题与内容间距 `48px`；移动区块间距 `64px`。
- **导航**：吸顶，滚动后加毛玻璃背景（`backdrop-blur` + 半透明 `--bg`）+ 底部 1px `--border`。
- **页脚**：4 列（产品 / 公司 / 资源 / 法律）+ 底部一行版权与备案占位，暗色下加深一档底色。

---

## 6. 组件规范

### 6.1 按钮

| 变体 | 样式 | 使用场景 |
| --- | --- | --- |
| Primary | `--brand` 实底、白字、8px 圆角、hover 上浮 1px + 品牌光晕 | 「免费试用」「预约演示」 |
| Secondary | 透明底 + 1px `--border` 描边 + `--fg` 文字，hover 底色 `--bg-subtle` | 次要动作 |
| Ghost | 无边框，hover 底色 `--bg-subtle` | 导航项 |
| Mono | 等宽字体 + Primary/Secondary 结构 | 终端式 CTA（如 `$ npm i loopwms` 一键复制） |

尺寸：高 40px（默认）/ 36px（紧凑）；圆角 8px；图标与文字间距 8px。

### 6.2 卡片

- 圆角 14px，1px `--border` 描边，底色 `--bg-subtle`。
- hover：描边变 `--brand` 40% 透明度、上浮 `translateY(-4px)`、投影 `0 12px 32px rgba(30,128,255,.10)`（暗色下投影改用黑色 30%）。
- 图标容器：40px 圆角方块（10px 圆角），底色 `--brand-soft`，前景品牌色图标；WMS/LIMS 卡片分别使用各自 accent。

### 6.3 徽章 / 标签

等宽字体 13px，`--brand-soft` 底 + 品牌色文字 + 1px 品牌色 25% 描边，圆角全圆。用于章节引导词（如 `01 · 产品`）。

### 6.4 代码 / 终端演示块（DeepSeek 式）

- 圆角 14px、`--bg-inset` 底、顶部三色圆点（macOS 风格：`#FF5F57` `#FEBC2E` `#28C840`）。
- 内部等宽 14px、行高 1.8、语法高亮（品牌蓝=关键字、accent=字符串、muted=注释）。
- 支持打字机流式输出动效，光标为品牌色方块闪烁。

### 6.5 导航与语言切换

- Logo（SVG 内联，继承 `currentColor`）+ 主导航（产品▾、解决方案、关于、文档）+ 右侧语言切换 + 主题切换 + CTA 按钮。
- 语言切换：`EN / 中` 下拉，选中项品牌色；切换后路由保持对应前缀（`/en/...` ↔ `/zh/...`）。
- 主题切换：太阳/月亮图标按钮，过渡 200ms，遵循系统偏好为默认值。
- 移动端：汉堡菜单展开全屏抽屉，含语言与主题切换。

---

## 7. 动效规范（重点参考 DeepSeek Harness）

### 7.1 原则

- 每个动效必须回答「它强调了什么信息」。
- 时长：微交互 150–200ms；入场/滚动 400–600ms；页面级过渡 ≤ 300ms。
- 缓动：入场用 `[0.22, 1, 0.36, 1]`（快出缓停）；位移类用 `ease-out`。
- 所有动效包裹在 `prefers-reduced-motion` 判断内，降级为直接显示。

### 7.2 标准动效清单

| 动效 | 参数 | 应用位置 |
| --- | --- | --- |
| **滚动渐入** | `opacity 0→1` + `translateY(24px)→0`，400ms，交错 80ms | 所有区块标题、卡片、图文分栏 |
| **章节编号浮现** | 等宽 `01 / 02 / 03` 从模糊 `blur(8px)` 到清晰 | 产品页章节（DeepSeek 式） |
| **数字滚动** | 进入视口后 800ms 内从 0 计数到目标值 | 数据指标条（仓位数、样本量、准确率） |
| **打字机流式输出** | 终端块内逐字符输出 + 品牌色方块光标闪烁 | WMS/LIMS 产品演示终端 |
| **代码行高亮扫过** | 品牌色 10% 背景条自上而下扫过 | 终端块关键行 |
| **卡片悬停光晕** | hover 时品牌色径向渐变跟随鼠标（`--x/--y` CSS 变量） | 特性卡片 |
| **Logo 环流** | 六边形 Logo 描边 `stroke-dashoffset` 循环流动 | 导航 Logo hover、页脚 |
| **主题切换** | 200ms 颜色过渡（`transition: background-color, color`） | 全局 |
| **滚动进度条** | 顶部 2px 品牌渐变进度条 | 长页面 |
| **Hero 粒子/网格漂移** | 背景网格 60s 匀速平移；光斑 8s 呼吸缩放 | Hero 区 |

### 7.3 禁止项

- 禁止 > 800ms 的入场动效；禁止自动播放的音频/视频；禁止视差幅度 > 40px；禁止布局位移型动效（scroll-jacking）。

---

## 8. 页面信息架构

```
/                     → 自动跳转 /zh 或 /en（按 Accept-Language）
/zh  /en              → 首页
  Hero（渐变点睛标题 + 三产品入口卡 + 数据指标条）
  三产品区（WMS / LIMS / ICMS 大卡片，accent 区分）
  工程实力（6 宫格特性卡）
  工作原理（编号章节 01/02/03 + 终端演示块）
  客户证言
  CTA（网格背景 + 光斑）
/zh/wms  /en/wms      → WMS 产品页
  Hero + 终端演示
  功能矩阵（入库/出库/库存/盘点/波次/报表）
  技术架构（开放 API、多仓、条码/RFID）
  指标 + CTA
/zh/lims  /en/lims    → LIMS 产品页
  同构，主题 accent 换为堇紫，功能矩阵换为（样品/检测/仪器/质控/报告/合规）
/zh/icms  /en/icms    → ICMS 产品页
  同构，主题 accent 换为琥珀，功能矩阵换为（房产/门禁/停车/收费/工单/公告）
/zh/about  /en/about  → 关于我们（itsalin 式极简单栏）
```

文案调性：中文克制专业、不堆形容词；英文简句主动语态。所有文案集中在 i18n 资源文件，禁止硬编码于组件。

---

## 9. 技术栈约定

| 项 | 选型 |
| --- | --- |
| 框架 | Next.js（App Router）+ TypeScript |
| 样式 | Tailwind CSS + CSS Variables（主题色全部走变量，`dark` class 切换） |
| 主题 | `next-themes`（系统默认 + 手动覆盖，localStorage 持久化） |
| 国际化 | `next-intl`（`/zh`、`/en` 路由前缀，Cookie 记忆） |
| 动效 | CSS 为主；复杂编排用 `motion`（framer-motion）；滚动触发用 `IntersectionObserver` 封装 `<Reveal>` 组件 |
| 图标 | `lucide-react`（线性 1.5px，与 Logo 描边语言一致） |
| 字体 | `next/font` 加载 Inter + JetBrains Mono |

组件文件全部为客户端可复用的服务端组件优先；仅交互组件（导航、终端、语言切换）标记 `"use client"`。

---

## 10. 验收清单（DoD）

- [ ] 明暗双主题逐页检查，无对比度不足（正文对比度 ≥ 4.5:1）
- [ ] 中英文逐页切换，无破版、无遗漏未翻译项
- [ ] `prefers-reduced-motion` 下动效全部降级
- [ ] 移动端 375px / 平板 768px / 桌面 1440px 三档响应式检查
- [ ] Lighthouse：Performance ≥ 90，Accessibility ≥ 95
- [ ] Logo 六边形在导航、页脚、favicon（`/icon.svg`）三处一致使用
