export type StatItem = { value: number; suffix: string; label: string };
export type FeatureItem = { icon: string; title: string; desc: string };
export type ChapterItem = { no: string; title: string; desc: string };
export type TestimonialItem = {
  quote: string;
  name: string;
  role: string;
  company: string;
};
export type ArchItem = { tag: string; title: string; desc: string };

export type Accent = "brand" | "wms" | "lims" | "icms";
